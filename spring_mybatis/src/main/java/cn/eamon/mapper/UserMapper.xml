<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.eamon.mapper.UserMapper">

    <!-- 根据ID获取用户信息 -->
    <select id="selectAllUser"
            resultType="cn.eamon.pojo.User" statementType="PREPARED" >
        select id, username name, birthday from user
    </select>
</mapper>